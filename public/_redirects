# SPA 라우팅.
#
# 이 앱은 index.html 하나로 도는 SPA 라서 /home 같은 경로에 해당하는 파일이 없습니다.
# 그래서 주소창 직접 입력, 새로고침, 외부 링크 진입(구글 캘린더 콜백 등) 때
# Netlify 가 파일을 못 찾아 404 를 냅니다.
#
# 어떤 경로든 index.html 을 200 으로 돌려주면 React Router 가 주소를 보고 화면을 그립니다.
# 301/302 로 하면 주소창이 / 로 바뀌어 어느 화면인지 알 수 없게 되므로 200 이어야 합니다.
/*    /index.html   200
